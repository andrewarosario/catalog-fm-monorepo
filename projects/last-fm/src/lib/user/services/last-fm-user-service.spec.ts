import { LastFmUserService } from './last-fm-user.service';

describe('LastFmUserService', () => {
  it('should be created', () => {
    const service = new LastFmUserService();
    expect(service).toBeTruthy();
  });
});
