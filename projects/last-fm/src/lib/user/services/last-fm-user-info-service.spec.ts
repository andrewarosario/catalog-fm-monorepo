import { LastFmUserInfoService } from './last-fm-user-info.service';

describe('LastFmUserInfoService', () => {
  it('should be created', () => {
    const service = new LastFmUserInfoService();
    expect(service).toBeTruthy();
  });
});
